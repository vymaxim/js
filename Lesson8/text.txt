1. Для практикума из занятия 7 продумать, где можно применить замыкания.

Затрудняюсь ответить, где можно использовать замыкания.

2. Не выполняя кода, ответить, что выведет браузер и почему:

if (!("a" in window)) {
    var a = 1;
}
alert(a);

undefined потому что не выполняется условие, 'а' не находится в window

-------------------------------------------------------------------------

var b = function a(x) {
    x && a(--x);
};
alert(a);

ошибка, потому что "а" не определена как переменная

-------------------------------------------------------------------------

function a(x) {
    return x * 2;
}
var a;
alert(a);

функция, потому что "а" является ссылкой на нее

-------------------------------------------------------------------------

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

10, потому что arguments[2] и есть "а", которой присваивают значение 10

-------------------------------------------------------------------------

function a() {
    alert(this);
}
a.call(null);

window

-------------------------------------------------------------------------